let initialState = {
    friends: [
        { id: 1, name: 'Vasya' },
        { id: 2, name: 'Larisa' },
        { id: 3, name: 'Anna' },
    ]
}

const navbarReducer = (state = initialState, action) => {
    return state;
}

export default navbarReducer;